// 函数组合子管理程序控制流
// 命令式编程中的循环与if-else

// 组合子是可以组合其他函数或其他组合子，并作为控制逻辑运行的高阶函数
// 组合子通常不需声明变量，同时也不包含业务逻辑，他们旨在管理函数式程序的控制流程。
// 常见的组合子如下:
// identity-tap-alternation-sequence-fork

// identity 组合子是返回与参数同值得函数
// identity :: a -> a
